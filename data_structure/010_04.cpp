#include <string>
#include <cmath>
#include <iostream>
#include <algorithm>
using namespace std;

struct tree {
    tree *nt[26];
    bool end;
    char v;
    int a[500000];
} a[50000];

int main() {
    int n;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        string s;
        cin >> s;
        for (int j = 0; j < s.size(); j++) {
            
        }
    }
    return 0;
}
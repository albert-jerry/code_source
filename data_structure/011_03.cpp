#include <string>
#include <cmath>
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int t;
    scanf("%d", &t);
    for (int it = 0; it < t; it++) {
        int zx, zy;
        int n;
        scanf("%d%d%d", &zx, &zy, &n);
        for (int i = 0; i < n; i++) {
            int x, y, r;
            scanf("%d%d%d", &x, &y, &r);
        }
        int ret = 0;
        cout << 1 << "\n";
    }
    return 0;
}